#include "kernel/types.h"
#include "kernel/stat.h"
#include "user/user.h"

int
main(int argc, char *argv[])
{
  // TODO: parse the sleep duration argument, call sleep(), and exit
  // with a non-zero status if the command is used incorrectly.
  exit(0);
}
