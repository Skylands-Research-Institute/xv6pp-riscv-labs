#include "kernel/types.h"
#include "kernel/stat.h"
#include "user/user.h"

int
main(int argc, char *argv[])
{
  // TODO: use pipe(), fork(), read(), write(), and wait() to send one
  // byte from parent to child and one byte back from child to parent.
  exit(0);
}
